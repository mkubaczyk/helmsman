context: team-b

settings:
  kubeContext: "minikube"

namespaces:
  team-b:
    protected: false
    labels:
      team: "team-b"
      env: "production"

helmRepos:
  ingress-nginx: "https://kubernetes.github.io/ingress-nginx"
  prometheus-community: "https://prometheus-community.github.io/helm-charts"
  podinfo: "https://stefanprodan.github.io/podinfo"

apps:
  # Priority -2: lightweight app — deploys first, fully namespace-scoped
  podinfo-team-b:
    namespace: "team-b"
    enabled: true
    chart: "podinfo/podinfo"
    version: "6.6.0"
    priority: -2
    alwaysUpgrade: true
    set:
      replicaCount: "2"
      ui.message: "team-b production"

  # Priority -1: ingress layer — deploys after infra
  nginx-team-b:
    namespace: "team-b"
    enabled: true
    chart: "ingress-nginx/ingress-nginx"
    version: "4.6.0"
    priority: -1
    alwaysUpgrade: true
    set:
      controller.ingressClassResource.name: "nginx-team-b"
      controller.ingressClass: "nginx-team-b"
      controller.replicaCount: "2"
      controller.metrics.enabled: "true"
      controller.autoscaling.enabled: "false"

  # Priority 0: observability — deploys last
  prometheus-team-b:
    namespace: "team-b"
    enabled: true
    chart: "prometheus-community/prometheus"
    version: "25.0.0"
    priority: 0
    alwaysUpgrade: true
    set:
      server.replicaCount: "1"
      server.retention: "12h"
      alertmanager.enabled: "false"
      prometheus-pushgateway.enabled: "false"
      serverFiles."prometheus\.yml".scrape_configs[0].job_name: "nginx-team-b"
