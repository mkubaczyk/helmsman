context: team-c

settings:
  kubeContext: "minikube"

namespaces:
  team-c:
    protected: false
    labels:
      team: "team-c"
      env: "development"

helmRepos:
  ingress-nginx: "https://kubernetes.github.io/ingress-nginx"
  prometheus-community: "https://prometheus-community.github.io/helm-charts"
  podinfo: "https://stefanprodan.github.io/podinfo"

apps:
  # Priority -2: lightweight app — deploys first, fully namespace-scoped
  podinfo-team-c:
    namespace: "team-c"
    enabled: true
    chart: "podinfo/podinfo"
    version: "6.6.0"
    priority: -2
    alwaysUpgrade: true
    set:
      replicaCount: "1"
      ui.message: "team-c development"

  # Priority -1: ingress layer — deploys after infra
  nginx-team-c:
    namespace: "team-c"
    enabled: true
    chart: "ingress-nginx/ingress-nginx"
    version: "4.6.0"
    priority: -1
    alwaysUpgrade: true
    set:
      controller.ingressClassResource.name: "nginx-team-c"
      controller.ingressClass: "nginx-team-c"
      controller.replicaCount: "1"
      controller.metrics.enabled: "false"

  # Priority 0: observability — deploys last
  prometheus-team-c:
    namespace: "team-c"
    enabled: true
    chart: "prometheus-community/prometheus"
    version: "25.0.0"
    priority: 0
    alwaysUpgrade: true
    set:
      server.replicaCount: "1"
      server.retention: "1h"
      alertmanager.enabled: "false"
      prometheus-pushgateway.enabled: "false"
      serverFiles."prometheus\.yml".scrape_configs[0].job_name: "nginx-team-c"
